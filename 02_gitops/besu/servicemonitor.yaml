apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: besu
  namespace: besu
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      besu-node: "true"
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics
