apiVersion: v1
kind: Service
metadata:
  name: ai-observability-agent
  namespace: ai
spec:
  type: NodePort
  selector:
    app: ai-observability-agent
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30810
