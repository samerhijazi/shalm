apiVersion: v1
kind: Service
metadata:
  name: quarkus-api
  namespace: quarkus-api
  labels:
    app: quarkus-api
spec:
  type: NodePort
  selector:
    app: quarkus-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30800
