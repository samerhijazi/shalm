apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orderer-data
  namespace: fabric
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
